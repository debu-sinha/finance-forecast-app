fastapi
uvicorn
pandas
numpy
prophet
mlflow
mlflow[databricks]
# mlflow[databricks] includes cloud-specific dependencies for Unity Catalog
# If you encounter "Unable to import necessary dependencies" errors when registering models,
# you may need to install the SDK for your cloud provider:
# - AWS: pip install boto3>=1.34.0
# - Azure: pip install azure-storage-blob>=12.0.0
# - GCP: pip install google-cloud-storage>=2.0.0
databricks-sdk>=0.28.0
scikit-learn
statsmodels
xgboost
pydantic
requests
python-dateutil  # Robust date parsing for data normalizer
openpyxl
holidays
python-multipart

# =============================================================================
# ADVANCED FORECASTING ENHANCEMENTS
# =============================================================================

# StatsForecast - Fast statistical models (AutoARIMA, AutoETS, AutoTheta)
# 10-100x faster than statsmodels/pmdarima
statsforecast>=1.7.0

# Chronos - Amazon's pretrained time series foundation model
# Requires PyTorch. For GPU acceleration:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
chronos-forecasting>=1.4.0
torch>=2.0.0

# MAPIE - Conformal prediction intervals with coverage guarantees
mapie>=0.8.0

# =============================================================================
# LAKEBASE POSTGRESQL (Multi-User Architecture)
# =============================================================================

# asyncpg - Fast async PostgreSQL driver for Lakebase
# Required for high-concurrency session management and job tracking
asyncpg>=0.29.0

# psycopg2-binary - Sync PostgreSQL driver (fallback)
# Use psycopg2 instead of psycopg2-binary in production for better performance
psycopg2-binary>=2.9.9

# Note: Ensemble and Conformal modules use only numpy/pandas (already installed)